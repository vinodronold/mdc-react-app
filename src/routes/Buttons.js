import React, { Component } from 'react';

class Buttons extends Component {
  render() {
    return (
      <div>
        <button type="button" className="mdc-button">Default</button>
      </div>
    );
  }
}

export default Buttons;
